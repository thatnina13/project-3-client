{{#each party as |party|}}
  <section data-id="{{party._id}}" class="party-log" >
    <h6>Date:</h6>
    <p>
      {{limit party.date 10}}
    <p>
    <h6>Title:</h6>
    <p>
        {{party.title}}
    </p>
    <h6>Details:</h6>
    <p>
        {{party.details}}
    </p>
  </section>

{{/each}}
